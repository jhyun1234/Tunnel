# 인계 문서 — 다음 세션은 여기서 시작한다

최종 갱신 2026-09-14. 이 문서 하나만 읽어도 이어서 작업할 수 있어야 한다.

## 1. 지금 어디까지 왔나

| | 상태 |
|---|---|
| Unity 프로젝트 | 빈 URP 프로젝트 생성 (6000.4.7f1, 템플릿 `3d-cross-platform` 17.0.14) |
| 저장소 | https://github.com/jhyun1234/Tunnel (Public, Git LFS) |
| 게임 코드·에셋 | 없음. Godot판 이식 전 |
| 빌드·검사 | `tools/build.sh` 없음. 배치 모드 빌드 이 PC에서 미검증 |

## 2. 다음에 할 일 (우선순위 순)

### A. 사람이 답해야 하는 것 — 사용자에게 물어야 진행 가능

1. CLAUDE.md "재미의 정체" 한 문장 확정
2. 첫 마일스톤 범위: 1인칭 이동 + 헤드램프 + 직선 갱도 조각 하나로 "조작해서 재미있는가" 판정용 씬부터인지
3. URP 볼류메트릭 안개 수단 (LOOK_REFERENCE 3-1: 기본 제공 안 됨, 별도 패키지 필요)

### B. 내가 할 수 있는 것

1. `tools/build.sh` — 배치 모드 Windows 빌드 + 로그 검사. 이 PC 첫 배치 빌드이므로 로그 끝 30줄 확인
2. 에셋 가져오기: 갱도 glTF 23개(`new-game/assets/generated/tunnel/`) — Unity는 glTF 기본 미지원이라 glTFast 패키지 또는 Blender에서 FBX 재수출 중 결정

## 3. 알고 있는 함정

- 에디터가 열려 있으면 배치 모드가 프로젝트 락에 걸린다.
- Godot 좌표는 오른손(-Z 앞), Unity는 왼손(+Z 앞). glTF 조각의 90° 회전 배치를 옮길 때 방향이 뒤집힐 수 있다.

## 4. 판정 대기

- 없음
